import NotFound from "../assets/NotFound";

export default function NotFoundPage() {
  return (
    <div className="w-full h-screen overflow-hidden bg-violet-900">
      <NotFound />
    </div>
  );
}
